package main

import (
	"log"
	"net/http"
    "goSamples/packages/handlers"
)

func main() {

	mux := http.NewServeMux()
	mux.Handle ("/get", handlers.getKey())
	mux.Handle ("/set", handlers.putKey())
	log.Fatal(http.ListenAndServe(":8080", mux))

}
